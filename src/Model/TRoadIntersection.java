package Model;

import java.awt.*;

public class TRoadIntersection extends Road {
    public TRoadIntersection(int roadId, int nextRoadId, int x, int y, int width, int height, Color color) {
        super(roadId, nextRoadId, x, y, width, height, color);
    }
}